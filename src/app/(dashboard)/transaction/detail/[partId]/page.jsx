import TransactionDetail from "@/components/transaksi/TransactionDetail"
export default async function TransactionDetailPage({ params }) {
    return (
        <TransactionDetail params={params} />
    )
  }